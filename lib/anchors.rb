# Implement the below functions in order.
module Anchors
  # Returns a regex that matches a string that has at least one line that has
  # exactly 1 word in it. It may have other lines that don't match this regex.
  def regex_with_at_least_one_line_that_has_exactly_one_word_in_it
    /WRITE ME/
  end

  # Returns a regex that matches a string that starts with "hello".
  def regex_matching_a_string_starting_with_hello
    /WRITE ME/
  end

  # Returns a regex that matches a string that starts with "goodbye".
  def regex_matching_a_string_ending_with_goodbye
    /WRITE ME/
  end

  # Returns a regex that matches "greeting" exactly.
  def regex_matching_greeting_exactly
    /WRITE ME/
  end

  # Returns a regex that matches exactly one word.
  def regex_matching_exactly_one_word
    /WRITE ME/
  end

  # Returns a regex that has the word "and" somewhere in it. This should match
  # "and", but not "demand", for example.
  def regex_matching_the_word_and
    /WRITE ME/
  end
end
